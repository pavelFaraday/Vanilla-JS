/* -------------------------------------------------------------------------- */
/*                               Default Exports                              */
/* -------------------------------------------------------------------------- */

/*
When we have modules that export only a single value or a function, than we can use 'default
 keyword while exporting.

When you use Default export method, in imported module name doesn't have to match to exported module name !!! 'fname' doesn't have to match to 'firstName';
*/

import firstName from './moduleD';
console.log(firstName);


// Alices


